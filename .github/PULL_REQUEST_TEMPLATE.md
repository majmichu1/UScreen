## What this changes

## Why

## How it was tested

Hardware, distribution, tablet. If latency is involved, the daemon's
`Latency encode→display` line before and after.

- [ ] `cargo test` and `cargo clippy` are clean
- [ ] Both sides updated if the app/daemon protocol changed
- [ ] README / docs updated if behaviour visible to users changed
